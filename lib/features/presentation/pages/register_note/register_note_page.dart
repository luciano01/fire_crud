import 'package:flutter/material.dart';

class RegisterNotePage extends StatelessWidget {
  const RegisterNotePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('RegisterNote'),
      ),
      body: Container(),
    );
  }
}
